apiVersion: v1
kind: Service
metadata:
  name: calendrer-ay
  namespace: calendar-ay
  labels:
    app: calendrer-ay
spec:
  type: ClusterIP
  selector:
    app: calendrer-ay
  ports:
    - name: http
      port: 80
      targetPort: 5000
